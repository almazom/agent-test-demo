---
name: Ask AI Assistant
on:
  workflow_dispatch:          # 👈 Запуск вручную (кнопка Run workflow)
    inputs:
      user_question:          # 👈 Параметр (переменная)
        description: "Что вы хотите узнать о коде?"
        required: true
        default: "Как работает этот проект?"

engine: copilot
permissions:
  contents: read

safe-outputs:
  create-issue:
    title-prefix: "[Ответ ИИ] "
---

# Инструкция для Агента

Ты — умный ассистент по этому проекту.
Твоя задача — ответить на вопрос пользователя.

1. **Вопрос:** "${{ inputs.user_question }}"
   *(Эта переменная заменится на текст, который введет пользователь)*

2. **Действия:**
   - Прочитай файлы в репозитории, чтобы найти ответ.
   - Создай Issue с подробным ответом.
   - Используй русский язык.